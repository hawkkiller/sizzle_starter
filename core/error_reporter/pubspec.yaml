name: error_reporter
description: Error reporter
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger:
    path: ../logger
  sentry_flutter: ^9.6.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
