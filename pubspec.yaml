name: root
description: "Workspace root"

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.32.0 <4.0.0"

dev_dependencies:
  sizzle_lints: ^2.1.7

workspace:
  - app
  # Core
  - core/analytics
  - core/logger
  - core/rest_client
  - core/error_reporter
  # Feature
  - feature/settings/settings_impl
  - feature/settings/settings_domain
  # - feature/settings/settings_presentation
