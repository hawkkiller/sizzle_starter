on:
  push:
    branches:
      - "feature/**"
      - "bugfix/**"
      - "hotfix/**"
      - "support/**"
      - "master"
  pull_request:
    branches:
      - "feature/**"
      - "bugfix/**"
      - "hotfix/**"
      - "support/**"
  workflow_dispatch:

jobs:
  test-analyze:
    runs-on: ubuntu-latest
    container:
      image: michaellazebny/flutter:master
      options: --user root

    steps:
      - name: 🫰 Check flutter version
        run: flutter --version

      - name: 🚂 Get latest code
        uses: actions/checkout@v2

      - name: 🐝 Dart tools activation
        run: |
          dart pub global activate coverage

      - name: 👷 Install Dependencies
        timeout-minutes: 1
        run: flutter pub get

      - name: 😊 Run codegen
        run: flutter pub run build_runner build --delete-conflicting-outputs

      - name: 🔎 Check format
        timeout-minutes: 1
        run: |
          find . -name '*.dart' -not -name 'firebase_options.dart' -not -path "*/.dart_tool/*" -not -name '*.*.dart' | xargs dart format -l 100 --set-exit-if-changed

      - name: 📈 Check analyzer
        timeout-minutes: 1
        run: |
          flutter analyze --fatal-infos --fatal-warnings

      - name: 🧪 Run tests
        timeout-minutes: 2
        run: |
          flutter test --coverage

      - name: 📥 Upload coverage to Codecov
        if: ${{ github.ref == 'refs/heads/main' }}
        timeout-minutes: 1
        uses: codecov/codecov-action@v2.1.0
