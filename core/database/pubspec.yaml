name: database
description: "Database for the app"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.3 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.29.0
  drift_flutter: ^0.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.27.0
  build_runner: ^2.5.3

flutter:
  uses-material-design: true